{
  "name": "cms-admin-node-typescript-ssr",
  "version": "0.2.0",
  "description": "Server-rendered, login-protected schema.org-aligned CMS admin UI in TypeScript on Node.js — no runtime dependencies.",
  "license": "MIT",
  "author": "Eric Binek",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node src/server.ts",
    "test": "node --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "5.9.2"
  },
  "private": true
}
